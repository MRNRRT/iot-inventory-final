APP_ENV=testing
APP_KEY=base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
APP_DEBUG=true
APP_URL=http://localhost

# Use in-memory SQLite just for tests
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# We don't need queues, mail, etc. for these tests
CACHE_STORE=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
LOG_CHANNEL=stack